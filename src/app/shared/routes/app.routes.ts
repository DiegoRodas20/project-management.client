import { ADMIN_PATH, APP_PATH, AUTH_PATH } from "./path.routes";

export const APP_ROUTES = {
    SIGN_IN: `/${APP_PATH.AUTH}/${AUTH_PATH.SIGN_IN}`,
    DASHBOARD: `/${APP_PATH.AUTH}/${ADMIN_PATH.DASHBOARD}`,
    INVOICES: `/${APP_PATH.AUTH}/${ADMIN_PATH.INVOICES}`,
    PROJECTS: `/${APP_PATH.AUTH}/${ADMIN_PATH.PROJECTS}`,
    TASKS: `/${APP_PATH.AUTH}/${ADMIN_PATH.TASKS}`,
}