import Toolbar from '@mui/material/Toolbar';
import { ReactNode } from 'react';

const MainHeader: ReactNode = (
    <Toolbar sx={{ px: { xs: 2, sm: 4.5, lg: 8 } }}></Toolbar>
)

export default MainHeader